/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',  // 정적 사이트로 내보내기
    basePath: '/NextR3F',  // GitHub Pages의 경로를 설정 (예: /NextR3F)
    assetPrefix: '/NextR3F/public',  
};

export default nextConfig;
